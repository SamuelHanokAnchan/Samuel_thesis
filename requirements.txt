# Runtime dependencies for the notebooks and the sanitised pipeline.
# Tested with Python 3.11 / 3.12.
numpy>=1.26
scikit-learn>=1.3
lightgbm>=4.0
matplotlib>=3.7
# For running the notebooks and the test suite:
jupyter>=1.0
pytest>=7.0
